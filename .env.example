# PostgreSQL connection string used by Drizzle (drizzle-kit) and the app.
# Locally this can point at any Postgres instance; on Vercel, Vercel Postgres
# provides POSTGRES_URL automatically (the app reads either variable).
DATABASE_URL="postgresql://user:password@localhost:5432/stokman"
